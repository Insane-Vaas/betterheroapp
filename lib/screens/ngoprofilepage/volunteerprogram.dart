import 'package:flutter/material.dart';

class VolunterProgramTilesWidget extends StatelessWidget {
  const VolunterProgramTilesWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: Text(
        "Feature Coming Soon",
        style: TextStyle(
          fontSize: 33,
        ),
      ),
    );
  }
}
